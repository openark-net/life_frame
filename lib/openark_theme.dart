import 'dart:ui';

class OpenArkColors {
  // Base colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color foreground = Color(0xFF000000);

  // Primary colors
  static const Color primary = Color(0xFF006AB5);
  static const Color primaryContrast = Color(0xFFFFFFFF);

  // Secondary colors
  static const Color secondary = Color(0xFF30A8E1);
  static const Color secondaryContrast = Color(0xFFFFFFFF);

  // Blue alternative
  static const Color blueAlt = Color(0xFFB9E1EC);
  static const Color blueAltContrast = Color(0xFF000000);

  // Tertiary colors
  static const Color tertiary = Color(0xFF00A49A);
  static const Color tertiaryContrast = Color(0xFFFFFFFF);

  // Green alternative
  static const Color greenAlt = Color(0xFF00AD68);
  static const Color greenAltContrast = Color(0xFFFFFFFF);

  // Warning colors
  static const Color warning = Color(0xFFB38600);
  static const Color warningContrast = Color(0xFFFFFFFF);

  // Error colors
  static const Color error = Color(0xFFFF3D3D);
  static const Color errorContrast = Color(0xFFFFFFFF);
}

// Font families
const String dmSansFont = "DM Sans";
const String dmSerifFont = "DMSerif";
